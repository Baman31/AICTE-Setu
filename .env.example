# MongoDB Configuration
# Get a free MongoDB database at https://www.mongodb.com/cloud/atlas
# Example: mongodb+srv://username:password@cluster.mongodb.net/aicte-setu
MONGODB_URI=your_mongodb_connection_string_here

# Session Secret (generate a random string for security)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_secret_key_here

# Node Environment (development or production)
NODE_ENV=development

# Server Port (default is 5000, change if needed)
PORT=5000
